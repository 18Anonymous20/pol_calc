stack =[]
while True:
    st = input()
    if st == 'q':
        break
    if st in ['+', '-', '*', '/']:
        op1 = stack.pop()
        op2 = stack.pop()
        if st == '+':
            stack.append(op1 + op2)
        if st == '-':
            stack.append(op1 - op2)
        if st == '*':
            stack.append(op1*op2)
        if st == '/':
            stack.append(op1/op2)
        print(stack[-1])
    else:
            stack.append(float(st))
